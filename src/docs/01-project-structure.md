# Frontend project structure

The current frontend structure is documented in ../../PROJECT_STRUCTURE.md.

The implementation uses feature-specific services, types, Zustand stores/slices, and pages. Shared layouts live in src/layouts; shared presentation lives in src/components/common and src/components/ui; route composition lives in src/routes/AppRoutes.tsx.

When this document conflicts with the source tree, the source tree wins.

